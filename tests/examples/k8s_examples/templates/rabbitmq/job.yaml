apiVersion: batch/v1
kind: Job
metadata:
  name: kimia-rabbitmq-build
  namespace: kimia
  labels:
    image: rabbitmq
spec:
  backoffLimit: 1
  ttlSecondsAfterFinished: 60
  template:
    spec:
      restartPolicy: Never
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
      - name: kimia-rabbitmq-build
        image: ghcr.io/rapidfort/kimia:latest
        imagePullPolicy: Always
        args:
          - --context=https://github.com/docker-library/rabbitmq.git
          - --dockerfile=4.2/alpine/Dockerfile
          - --destination=rabbitmq
          - -v
        resources:
          limits:
            cpu: "1"
            memory: "1Gi"  
        securityContext:
          allowPrivilegeEscalation: true
          capabilities:
            drop: [ALL]
            add: [SETUID, SETGID]

