apiVersion: batch/v1
kind: Job
metadata:
  name: kimia-rust-build
  labels:
    image: rust
spec:
  backoffLimit: 1
  ttlSecondsAfterFinished: 60
  template:
    spec:
      restartPolicy: Never
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
      - name: kimia-rust-build
        image: ghcr.io/rapidfort/kimia-bud:1.0.18
        imagePullPolicy: Always
        args:
          - --context=https://github.com/rust-lang/docker-rust.git
          - --dockerfile=stable/alpine3.21/Dockerfile
          - --destination=rust
          - --no-push
          - -v
        securityContext:
          allowPrivilegeEscalation: true
          capabilities:
            drop: [ALL]
            add: [SETUID, SETGID]
