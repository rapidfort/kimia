apiVersion: batch/v1
kind: Job
metadata:
  generateName: kimia-kimia-build
spec:
  backoffLimit: 1
  ttlSecondsAfterFinished: 60
  template:
    spec:
      restartPolicy: Never
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
      - name: smithy
        image: ghcr.io/rapidfort/kimia-bud:latest
        imagePullPolicy: Always
        args:
          - --context=https://github.com/rapidfort/kimia.git
          - --dockerfile=Dockerfile.buildkit
          - --destination=harbor.rfinnovate.rapidfort.io/kimia-e2e/kimia
          - --git-branch=tests-kimia
          - --git-revision=6406608ad3e241a2dbb7f79fb737e67ec9577411
          - --no-push
          - -v=debug
        securityContext:
          allowPrivilegeEscalation: true
          capabilities:
            drop: [ALL]
            add: [SETUID, SETGID]
