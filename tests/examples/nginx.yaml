apiVersion: batch/v1
kind: Job
metadata:
  name: kimia-nginx-build
spec:
  backoffLimit: 1
  ttlSecondsAfterFinished: 60
  template:
    spec:
      restartPolicy: Never
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
      - name: kimia
        image: 10.228.96.114:5000/rapidfort/kimia:latest
        imagePullPolicy: Always
        args:
          - --context=https://github.com/nginx/docker-nginx.git
          - --context-sub-path=""
          - --dockerfile=mainline/alpine/Dockerfile
          - --destination=mynginx
          - --no-push
        securityContext:
          allowPrivilegeEscalation: true
          capabilities:
            drop: [ALL]
            add: [SETUID, SETGID]